
Процедура ПрочитатьДанныеОбъектаИзМакета()
	ФайлДанных = ПолучитьФайлДанных();
	
	Читатель = Новый ЧтениеJSON;
	
	Читатель.ОткрытьФайл(ФайлДанных);

	Объект = СериализаторXDTO.ПрочитатьJSON(Читатель);
	
	СсылкаНового = Документы.НачислениеЗарплаты.ПолучитьСсылку(Новый УникальныйИдентификатор);
	
	Объект.УстановитьСсылкуНового(СсылкаНового);
	
	Объект.Записать(РежимЗаписиДокумента.Проведение);
	
	Читатель.Закрыть();	
КонецПроцедуры

Функция ПолучитьФайлДанных() 
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();

	ДвоичныеДанные = ПолучитьМакет("Объект");
	
	ДвоичныеДанные.Записать(ИмяВременногоФайла);
	
	Возврат ИмяВременногоФайла;
КонецФункции

ПрочитатьДанныеОбъектаИзМакета();