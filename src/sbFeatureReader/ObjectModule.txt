
Процедура ПрочитатьДанныеЛокализации()
	ФайлДанныхЛокализации = ПолучитьФайлДанныхЛокализации();
	
	Читатель = Новый ЧтениеJSON;
	
	Читатель.ОткрытьФайл(ФайлДанныхЛокализации);
	Пока Читатель.Прочитать() Цикл
		
	КонецЦикла;
КонецПроцедуры

Функция ПолучитьФайлДанныхЛокализации() 
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();

	ДвоичныеДанные = ПолучитьМакет("Languages");
	
	ДвоичныеДанные.Записать(ИмяВременногоФайла);
	
	Возврат ИмяВременногоФайла;
КонецФункции

ПрочитатьДанныеЛокализации();